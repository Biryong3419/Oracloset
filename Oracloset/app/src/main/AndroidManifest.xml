<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.woocheol.oracloset">

    <application
        android:name=".Tools.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Register.RegistJobActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Register.RegistInfoActivity" />
        <activity android:name=".Register.RegistGenderActivity" />
        <activity android:name=".Register.RegistAgeActivity" />
        <activity android:name=".Login.LoginActivity"/>

        <activity android:name=".Register.RegistStyleActivity" />
        <activity android:name=".Register.RegistGraphActivity"></activity>
    </application>

</manifest>